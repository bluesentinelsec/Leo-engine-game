name: Cross-Platform Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-latest, macos-latest]
        build_type: [debug, release, dist]
        
    steps:
    - uses: actions/checkout@v4
    
    - name: Install dependencies (Ubuntu)
      if: matrix.os == 'ubuntu-22.04'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake libsdl2-dev xvfb xauth

    - name: Set up virtual framebuffer (Ubuntu)
      if: matrix.os == 'ubuntu-22.04'
      run: |
        export DISPLAY=:99
        nohup Xvfb :99 -screen 0 1280x720x24 >/dev/null 2>&1 &
        echo "DISPLAY=${DISPLAY}" >> $GITHUB_ENV
        
    - name: Build ${{ matrix.build_type }}
      run: make ${{ matrix.build_type }}
      
    - name: Test debug build (Windows)
      if: matrix.build_type == 'debug' && matrix.os == 'windows-latest'
      run: |
        .\build-debug\bin\Debug\leo-pong.exe --one-frame
        
    - name: Test debug build (Unix)
      if: matrix.build_type == 'debug' && matrix.os != 'windows-latest'
      run: |
        ./build-debug/leo-pong --one-frame
        
    - name: Test release build (Windows)
      if: matrix.build_type == 'release' && matrix.os == 'windows-latest'
      run: |
        .\build-release\bin\Release\leo-pong.exe --one-frame
        
    - name: Test release build (Unix)
      if: matrix.build_type == 'release' && matrix.os != 'windows-latest'
      run: |
        ./build-release/leo-pong --one-frame
        
    - name: Test dist build (Windows)
      if: matrix.build_type == 'dist' && matrix.os == 'windows-latest'
      run: |
        .\build-release-dist\bin\Release\leo-pong.exe --one-frame
        
    - name: Test dist build (Unix)
      if: matrix.build_type == 'dist' && matrix.os != 'windows-latest'
      run: |
        if [ "${{ matrix.os }}" = "macos-latest" ]; then
          ./build-release-dist/leo-pong.app/Contents/MacOS/leo-pong --one-frame
        else
          ./build-release-dist/leo-pong --one-frame
        fi
        
    - name: Upload debug artifact
      if: matrix.build_type == 'debug'
      uses: actions/upload-artifact@v4
      with:
        name: leo-pong-${{ matrix.os == 'ubuntu-22.04' && 'linux-amd64' || matrix.os == 'windows-latest' && 'windows-amd64' || 'macos-universal' }}-debug
        path: |
          ${{ matrix.os == 'windows-latest' && 'build-debug/bin/Debug/leo-pong.exe' || 'build-debug/leo-pong' }}
          
    - name: Upload release artifact
      if: matrix.build_type == 'release'
      uses: actions/upload-artifact@v4
      with:
        name: leo-pong-${{ matrix.os == 'ubuntu-22.04' && 'linux-amd64' || matrix.os == 'windows-latest' && 'windows-amd64' || 'macos-universal' }}-release
        path: |
          ${{ matrix.os == 'windows-latest' && 'build-release/bin/Release/leo-pong.exe' || 'build-release/leo-pong' }}
          
    - name: Upload distribution artifact
      if: matrix.build_type == 'dist'
      uses: actions/upload-artifact@v4
      with:
        name: leo-pong-${{ matrix.os == 'ubuntu-22.04' && 'linux-amd64' || matrix.os == 'windows-latest' && 'windows-amd64' || 'macos-universal' }}-dist
        path: dist/
